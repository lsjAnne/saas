# 关键流程泳道图文档

| 文档名称 | 文档类型 | 当前版本 | 适用对象 | 主要读者 | 维护状态 |
| --- | --- | --- | --- | --- | --- |
| 关键流程泳道图文档 | 架构/流程设计 | v1.1 | 核心链路评审、研发对齐、测试验收 | 产品、架构、研发、测试、平台运营 | 已整理 |

## 1. 文档目的
本文档将商用版最关键的跨系统流程整理为泳道图，用于明确 `SaaS 平台底座 + OMS + SRM + ERP + CRM + WMS/TMS + BI` 协同过程中的角色边界、系统职责、校验点与留痕要求。

## 2. 泳道图说明
- 泳道按真实参与角色拆分，不按代码模块拆分。
- 重点观察“谁发起、谁校验、谁执行、谁留痕、谁兜底”。
- 详细字段、状态和接口定义仍以专题设计文档为准。

## 3. 租户开通与订阅初始化
```mermaid
flowchart LR
    subgraph U1[客户管理员]
        A1[提交开通申请]
        A2[选择试用或正式套餐]
    end
    subgraph O1[平台运营]
        B1[审核客户信息]
        B2[确认套餐和席位策略]
    end
    subgraph S1[SaaS平台底座]
        C1[创建租户与默认组织]
        C2[初始化订阅 配额 席位]
        C3[写入审计日志并通知]
    end

    A1 --> A2 --> B1 --> B2 --> C1 --> C2 --> C3
```

## 4. 店铺接入与 OMS 初始化
```mermaid
flowchart LR
    subgraph U2[租户管理员]
        D1[发起店铺接入]
        D2[补充经营参数]
    end
    subgraph P2[电商平台]
        E1[返回授权页]
        E2[回传授权结果]
    end
    subgraph S2[SaaS底座与OMS]
        F1[校验订阅 配额 店铺上限]
        F2[生成接入任务]
        F3[保存授权信息]
        F4[初始化订单同步]
        F5[记录审计与通知]
    end

    D1 --> F1 --> F2 --> E1 --> D2 --> E2 --> F3 --> F4 --> F5
```

## 5. 补货建议到采购协同
```mermaid
flowchart LR
    subgraph O3[租户运营]
        G1[查看库存预警]
        G2[确认补货申请]
    end
    subgraph S3[ERP与SRM]
        H1[生成库存快照]
        H2[计算补货建议]
        H3[匹配供应商与价格]
        H4[生成采购协同单]
    end
    subgraph P3[供应商]
        I1[确认交期与供货]
    end
    subgraph A3[审批人]
        J1[审批采购协同]
    end

    G1 --> H1 --> H2 --> G2 --> H3 --> H4 --> J1 --> I1
```

## 6. 订单履约与异常补偿
```mermaid
flowchart LR
    subgraph P4[电商平台]
        K1[订单回传]
    end
    subgraph S4[OMS]
        L1[验签 幂等校验]
        L2[审单与路由]
        L3[生成履约任务]
        L4[识别异常]
    end
    subgraph W4[仓储与物流]
        M1[出库与发运]
        M2[物流回传]
    end
    subgraph O4[运营人员]
        N1[处理异常或补偿]
    end

    K1 --> L1 --> L2 --> L3 --> M1 --> M2 --> L4
    L4 -->|正常| M2
    L4 -->|异常| N1 --> L3
```

## 7. 客户服务辅助与人工接管
```mermaid
flowchart LR
    subgraph B5[买家]
        O1[发起咨询]
    end
    subgraph S5[CRM与模型辅助层]
        P1[识别问题类型]
        P2[检索知识库]
        P3[生成回复建议]
        P4[执行风险校验]
    end
    subgraph C5[客服人员]
        Q1[采纳建议或转人工]
        Q2[发送正式回复]
    end
    subgraph G5[治理能力]
        R1[记录会话 审计 建议留痕]
    end

    O1 --> P1 --> P2 --> P3 --> P4
    P4 -->|低风险| Q1 --> Q2 --> R1
    P4 -->|高风险| Q1 --> Q2 --> R1
```

## 8. 退租与数据清理
```mermaid
flowchart LR
    subgraph U6[租户管理员]
        S1[提交退租申请]
        S2[在窗口期申请导出]
    end
    subgraph O6[平台运营]
        T1[校验账单 工单 争议状态]
        T2[审批退租与导出]
    end
    subgraph G6[治理系统]
        U1[冻结新增写入]
        U2[执行异步导出]
        U3[进入待清理状态]
        U4[按tenant_id清理]
        U5[保留最小审计与财务记录]
        U6[输出清理结果日志]
    end

    S1 --> T1 --> T2 --> U1 --> S2 --> U2 --> U3 --> U4 --> U5 --> U6
```

## 9. 标准使用方式
- 产品评审时，先用本文件确认流程闭环，再回到需求和接口文档补齐细节。
- 开发排期时，按泳道图拆分前端、后端、模型能力和测试任务。
- 测试验收时，将每条泳道上的关键节点转成测试用例。

## 10. 关联文档
- [产品需求文档](../../requirements/product/01-产品需求文档.md)
- [角色与权限说明](03-角色与权限说明.md)
- [多租户SaaS隔离与平台运营设计整理](09-多租户SaaS隔离与平台运营设计整理.md)
- [租户数据保留与退租清理策略](11-租户数据保留与退租清理策略.md)
- [SDD开发设计文档](10-SDD开发设计文档.md)
